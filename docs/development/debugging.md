# Debugging

Debugging guidance will be added with the first runnable target. It must describe reproducible logging, render diagnostics, document recovery, platform-specific launch commands, and how to capture enough context without exposing user images or metadata.

Do not add logging that silently records image contents, file paths, metadata, or credentials.
