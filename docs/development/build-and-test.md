# Build and test

Canonical commands:

```shell
./scripts/check.sh
./scripts/test.sh
./scripts/format.sh
./scripts/verify-architecture.sh
```

During the scaffold phase, these commands validate Gradle configuration, documentation links and structure, and architecture boundaries that can be checked without source code. They will expand as Kotlin source, tests, formatting, and static analysis are introduced.
